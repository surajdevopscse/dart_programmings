import 'package:dart_programing/app/device_screen_type.dart';

typedef AS = AppService;

class AppService {
  AppService._privateConstructor();
  static DeviceScreenType deviceScreenType = DeviceScreenType.unidentified;
}
