// Mobile / desktop stub — renders nothing.
import 'package:flutter/material.dart';

class WebAdWidget extends StatelessWidget {
  const WebAdWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
